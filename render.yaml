services:
  - type: web  
    name: US-visa-bot # A more descriptive name for a web service
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node your_main_bot_file.js # Ensure this points to your actual main file
    